const isUserActive = true;
const message = isUserActive ? "User is active" : "User is not active";

console.log(message);

const knowledge = "JavaScript";
const canIStartWithReact = knowledge === "JavaScript" && true;

console.log(canIStartWithReact);