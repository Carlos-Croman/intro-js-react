const degree = "Computer Systems Engineer";
const fullname = "Carlos Croman";

const whoAmI = `Hi, i'm ${fullname} and i'm a ${degree}.`;

console.log(whoAmI);
console.log(`String template can interpolate strings with javascript expressions, like this: ${1 + 1}.`);